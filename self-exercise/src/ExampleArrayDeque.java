import java.util.ArrayDeque;

public class ExampleArrayDeque {
  public static void main(String[] args) {

    // // Create an ArrayDeque of Integers
    // ArrayDeque<Integer> deque = new ArrayDeque<>();
    // // Adding elements to the front and back of the deque
    // deque.addFirst(10);
    // deque.addLast(20);
    // deque.addLast(30);

    // ArrayDeque<Integer> deque = new ArrayDeque<>();
    // deque.addFirst(10);
    // int firstElement = deque.removeFirst();

    // ArrayDeque<Integer> deque = new ArrayDeque<>();
    // deque.addLast(20);
    // int lastElement = deque.removeLast();

    // ArrayDeque<Integer> deque = new ArrayDeque<>();
    // deque.addFirst(10);
    // Integer firstElement = deque.pollFirst();

    // ArrayDeque<Integer> deque = new ArrayDeque<>();
    // deque.addLast(20);
    // Integer lastElement = deque.pollLast();

    // ArrayDeque<Integer> deque = new ArrayDeque<>();
    // // int firstElement = deque.getFirst(); // java.util.NoSuchElementException
    // deque.addFirst(10);
    // int firstElement = deque.getFirst();

    // ArrayDeque<Integer> deque = new ArrayDeque<>();
    // // int lastElement = deque.getLast(); // java.util.NoSuchElementException
    // deque.addLast(20);
    // int lastElement = deque.getLast();

    // ArrayDeque<Integer> deque = new ArrayDeque<>();
    // Integer firstElement = deque.peekFirst();
    // deque.addFirst(10);
    // firstElement = deque.peekFirst();

    // ArrayDeque<Integer> deque = new ArrayDeque<>();
    // Integer lastElement = deque.peekLast();
    // deque.addLast(20);
    // lastElement = deque.peekLast();

    // ArrayDeque<Integer> deque = new ArrayDeque<>();
    // deque.addFirst(10);
    // deque.addLast(20);
    // deque.addLast(30);
    // // deque.removeFirstOccurrence(20);
    // deque.removeLastOccurrence(20);




    


  }
}
