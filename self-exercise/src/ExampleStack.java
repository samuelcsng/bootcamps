import java.util.Stack;

public class ExampleStack {
  public static void main(String[] args) {

    // // Create a Stack of Integers
    // Stack<Integer> stack = new Stack<>();
    // // Adding elements to the stack
    // stack.push(10);
    // stack.push(20);
    // stack.push(30);

    // Stack<Integer> stack = new Stack<>();
    // stack.push(10);
    // stack.push(20);
    // stack.set(0, 5); // Replaces 10 with 5 at index 0

    // Stack<Integer> stack = new Stack<>();
    // stack.push(18);
    // stack.push(28);
    // stack.removeAllElements(); // Removes all elements from the stack

    // Stack<Integer> stack = new Stack<>();
    // stack.push(10);
    // stack.remove(10); // Removes the element 10 from the stack

    Stack<Integer> stack = new Stack<>();
    stack.add(10);
    stack.add(20);
    stack.clear(); // Removes all elements from the stack

  }
}
