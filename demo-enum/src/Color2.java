public enum Color2 {
  // enum values (objects)
  RED, BLUE; // create 2 objects - static final (one and only one)

  // Normal Class Section
  // attributes (can be no attribute)
  // constructor
  // methods

  // private Color2(){ // constructor is default, no new defined

  // }
  
  // the meaning of enum:
  // JVM will help create the Color2 Object(RED, BLUE)
  // As user, you cannot create the enum object by yourself
  
}
